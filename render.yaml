services:
  - type: web
    name: jackie-peanuts
    runtime: node
    buildCommand: bash render-build.sh
    startCommand: node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production

